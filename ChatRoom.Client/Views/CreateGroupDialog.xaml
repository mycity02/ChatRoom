<UserControl x:Class="ChatRoom.Client.Views.CreateGroupDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ChatRoom.Client.Views"
             mc:Ignorable="d" 
             d:DesignHeight="360" d:DesignWidth="460">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="创建群聊"
                   FontSize="18" FontWeight="Bold"/>

        <TextBox Grid.Row="1" Height="36"
                   Text="{Binding GroupName, UpdateSourceTrigger=PropertyChanged}"
                   Margin="0,16,0,12" Padding="8,0"
                   VerticalAlignment="Center"/>
        <ListBox Grid.Row="2"
                 ItemsSource="{Binding FriendCollection}"
                 BorderBrush="#E0E0E0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox Content="{Binding UserName}"
                              IsChecked="{Binding IsSelected, Mode=TwoWay}"
                              Padding="8" FontSize="13"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="取消"
                    Width="72"
                    Height="32"
                    Margin="0,0,8,0"
                    Command="{Binding CancelCommand}"/>

            <Button Content="创建"
                    Width="72"
                    Height="32"
                    Command="{Binding ConfirmCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
