<Window x:Class="ChatRoom.Client.Views.ChatView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChatRoom.Client.Views"
        mc:Ignorable="d"
        Title="ChatView" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        
        <!-- 顶部：连接状态栏  -->
        <Border Grid.Row="0" Background="#F5F5F5" Padding="12,8" 
                BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <TextBlock FontSize="14" 
                           VerticalAlignment="Center" Margin="0,0,6,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="●"/>
                            <Setter Property="Foreground" Value="#F44336"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                    <Setter Property="Foreground" Value="#4CAF50"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <TextBlock FontSize="13" VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="未连接"/>
                            <Setter Property="Foreground" Value="#666"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                    <Setter Property="Text" Value="已连接"/>
                                    <Setter Property="Foreground" Value="#4CAF50"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                
                <Button Content="连接" Command="{Binding ConnectCommand}"
                        Width="60" Height="26" 
                        Margin="12,0,0,0" FontSize="12"/>
                
                <TextBlock Text="{Binding UserName}" FontSize="13" 
                           VerticalAlignment="Center" Margin="6,0,0,0" Foreground="#2196F3"/>
            </StackPanel> 
        </Border>
        
        <!-- 中间：聊天消息列表  -->
        <ListBox Grid.Row="1" 
                 ItemsSource="{Binding MessageCollection}"
                 BorderThickness="0" Background="White"
                 Padding="6">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Padding="6,3">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding UserName}" FontWeight="Bold" FontSize="13"
                                       Foreground="#2196F3"/>
                            <TextBlock Text="：" FontSize="13" Foreground="#999"/>
                            <TextBlock Text="{Binding Content}" FontSize="13"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        
        <!-- 底部：输入区域 -->
        <Border Grid.Row="2" Background="#FAFAFA" 
                Padding="12" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">

            <!-- 消息输入行 -->
            <StackPanel Grid.Row="1" Orientation="Horizontal">
                <TextBlock Text="消息" FontSize="13"
                           VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBox Text="{Binding Message, UpdateSourceTrigger=PropertyChanged}"
                         Width="400" Height="28" FontSize="13"
                         VerticalAlignment="Center"/>
                <Button Content="发送" Command="{Binding SendCommand}"
                        Width="60" Height="28" 
                        Margin="8,0,0,0" FontSize="12"/>
            </StackPanel>
           
        </Border>
    </Grid>
</Window>
